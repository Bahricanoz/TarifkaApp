import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container : {
        borderRadius : 5,
        backgroundColor : "red",
        padding : 10,
    },
    text: {
        textAlign : "center",
        color : "#fff",
        fontSize : 20,
    }
})